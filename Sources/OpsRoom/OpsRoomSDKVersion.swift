/// Version of this SDK binary, sent to the config API as the `sdkVersion` query parameter.
public enum OpsRoomSDKVersion {
    /// Current SDK version string (semver-style, e.g. `0.1.0`).
    public static let current = "0.1.0"
}
