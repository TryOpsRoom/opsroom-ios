name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  swift:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v6

      - name: Build and test Swift package
        run: swift build && swift test --no-parallel
